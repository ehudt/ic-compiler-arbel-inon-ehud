class A {
	static void main(string[] args) {
		int x;
		x = 2 + (3 + (4 + (5 + (6 + 7)))); //should take few registers
		Library.printi(x);
		Library.println("");
		x = (((((1 + 2) + 3) + 4) + 5) + 6) + 7;
		Library.printi(x);
		Library.println("");
		x = 2 + (((3 + 4) + (5 + (6 + 7))) + 8); //should take few registers
		Library.printi(x);
		Library.println("");
		x = -(-(-(-(-(-(-(5))))))); //should take few registers
		Library.printi(x);
		Library.println("");
	}
}
